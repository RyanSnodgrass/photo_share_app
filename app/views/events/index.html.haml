%nav.row
  .large-12.columns
    %h2 Events Index
    %ul.button-group.radius.even-4
      %li
        %a.button{href: "/"} Home
      %li
        %a.button{href: "/photos"} Photos
      %li
        %a.button{href: "/events"} Events
      %li
        %a.button{href: "/tags"} Tags
%h1 list of all events
- @events.each do |e|
	= e.name
	= e.location
	= e.date
	= link_to "Show", event_path(e)
	%br
%br
= button_to "New", new_event_path, :method => :get
